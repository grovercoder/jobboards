#!/bin/bash

SSH_PORT=22
SSH_KEY=/home/$USER/.ssh/id_ed25519
SSH_USER=REMOTE_USER
SSH_HOST=REMOTE_SERVER
REMOTE_PATH=REMOTE_FILEPATH
LOCAL_PATH=../dist

# copy the CSV file into the dist folder
scp -P $SSH_PORT -i $SSH_KEY -r $LOCAL_PATH/* $SSH_USER@$SSH_HOST:$REMOTE_PATH
